@model IEnumerable<Absence.Models.T_Etudiant>

<h2>Students</h2>
<a asp-action="Create" class="btn btn-primary">Add New Student</a>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Class</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.Nom @student.Prenom</td>
                <td>@student.Classe.NomClasse</td>
                <td>@student.Mail</td>
                <td>@student.Tel</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@student.CodeEtudiant" class="btn btn-warning">Edit</a>
                    <a asp-action="Delete" asp-route-id="@student.CodeEtudiant" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>